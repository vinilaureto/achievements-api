export module globalConfiguration {
  // API port number
  export const API_PORT: number = 0;

  // URL to connect to MongoDB
  export const DATABASE_URL_CONNECTION: string = ""

  // Secret key for Json Web Token autheticate
  export const JWT_SALT_KEY: string = ""
}
